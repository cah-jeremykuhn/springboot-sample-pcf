---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: '3-jdk-8'

inputs:
- name: source-code

outputs:
- name: build-output

run:
  path: mvn
  args: ["-f", "-e", "source-code/pom.xml", "package"]
  path: mv
  args: ["source-code/target/*.jar","build-output/."]